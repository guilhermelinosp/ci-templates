name: release-please

on:
  workflow_call:

permissions:
  contents: write
  pull-requests: write

jobs:
  release:
    runs-on: self-hosted
    steps:
      - uses: googleapis/release-please-action@v4
        with:
          release-type: simple
          skip-github-pull-request: false
