name: snyk

# Snyk vulnerability scanning
# Secrets: SNYK_TOKEN (required)
# Inputs: project-type (default: dotnet), severity (default: high), monitor (default: false)

on:
  workflow_call:
    inputs:
      project-type:
        required: false
        default: "dotnet"
        type: string
      severity:
        required: false
        default: "high"
        type: string
      monitor:
        required: false
        default: false
        type: boolean
    secrets:
      SNYK_TOKEN:
        required: false

permissions:
  contents: read
  security-events: write

jobs:
  scan:
    runs-on: ubuntu-latest
    if: ${{ secrets.SNYK_TOKEN != '' }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Snyk test
        uses: snyk/actions/${{ inputs.project-type }}@v1
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          args: --severity-threshold=${{ inputs.severity }} --sarif-file-output=snyk.sarif

      - name: Snyk monitor
        if: ${{ inputs.monitor }}
        uses: snyk/actions/${{ inputs.project-type }}@v1
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          command: monitor
          args: --severity-threshold=${{ inputs.severity }}

      - name: Upload SARIF
        if: always()
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: snyk.sarif
