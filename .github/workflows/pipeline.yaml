name: pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: write
  packages: write
  id-token: write

jobs:
  login:
    uses: ./.github/workflows/login.yaml
    secrets:
      GHCR_PAT: ${{ secrets.GHCR_PAT }}
