name: branch-rules

on:
  workflow_call:

permissions:
  contents: read
  pull-requests: write

jobs:
  validate:
    runs-on: self-hosted
    steps:
      - run: |
          H="${{ github.head_ref }}"; B="${{ github.base_ref }}"
          echo "PR: $H → $B"
