name: buildx

on:
  workflow_call:
    inputs:
      runner:
        required: true
        type: string
      registry_owner:
        required: true
        type: string
    outputs:
      image:
        value: ${{ jobs.buildx.outputs.image }}

permissions:
  contents: read
  packages: write

jobs:
  buildx:
    runs-on: ${{ inputs.runner }}
    outputs:
      image: ${{ steps.export.outputs.image }}
    steps:
      - uses: actions/checkout@v6

      - name: Login GHCR
        env:
          DOCKER_CONFIG: /tmp/.docker
        run: |
          mkdir -p /tmp/.docker
          TOKEN="${{ secrets.GHCR_PAT }}"
          if [ -z "$TOKEN" ]; then TOKEN="${{ secrets.GITHUB_TOKEN }}"; fi
          echo "$TOKEN" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Setup buildx
        run: |
          docker buildx create --name ci-builder --driver docker-container \
            --config /dev/null --bootstrap --use 2>/dev/null || docker buildx use ci-builder

      - name: Build and push
        run: |
          IMG="ghcr.io/${{ inputs.registry_owner }}/${{ github.event.repository.name }}"
          docker buildx build \
            --platform linux/amd64 \
            --push \
            --attest type=sbom \
            --attest type=provenance,mode=max \
            --cache-from type=registry,ref=${IMG}:cache \
            --cache-to type=registry,ref=${IMG}:cache,mode=max \
            --metadata-file /tmp/metadata.json \
            -t "${IMG}:latest" .

      - name: Export digest
        id: export
        run: |
          DIGEST=$(jq -r '."containerimage.digest"' /tmp/metadata.json)
          echo "image=ghcr.io/${{ inputs.registry_owner }}/${{ github.event.repository.name }}@${DIGEST}" >> $GITHUB_OUTPUT
